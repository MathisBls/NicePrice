name: Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version for this release (e.g. 1.0.2)'
        required: true
        default: '0.0.0'

permissions:
  contents: write
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Install Dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Package Build Output
        run: |
          zip -r nice-price-v${{ github.event.inputs.version }}.zip .millennium backend plugin.json README.md

      - name: Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ github.event.inputs.version }}
          files: nice-price-v${{ github.event.inputs.version }}.zip
          draft: true
